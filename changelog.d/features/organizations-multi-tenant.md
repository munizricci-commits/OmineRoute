- feat(org): native multi-tenant Organizations — users belong to one or more organizations, each with its own connections, combos, and an optional org-wide quota. Organization resources are addressed through qualified routes (`<organization>/<route>` where `<route>` ∈ {combo name, `auto:<strategy>`, provider model id}), which reuse the existing `auto`/`combo` machinery (no second routing engine). Roles: `user` / `moderator` / `owner` / `platform_admin`; authorization is fail-closed (non-members rejected 404/403, default deny). Additive migrations 155/158/159/161; legacy personal routing, personal API-key auth, and remote management tokens are unchanged. Secret redaction and cross-tenant isolation enforced server-side (UI visibility is never a security boundary). (#orgs)
